[FORMAT]

# Maximum number of characters on a single line.
max-line-length=99

[BASIC]

# Good variable names which should always be accepted, separated by a comma
good-names=c,e,n,w,r,ui

# Constants are lowercased characters
const-rgx=(([a-z_][a-z0-9_]*)|(__.*__))$
const-name-hint=(([a-z_][a-z0-9_]*)|(__.*__))$

# Classes are lowercased character (like any functions)
class-rgx=[a-z_][a-z0-9_]+$
class-name-hint=[a-z_][a-z0-9]+$

# Modules should respect classic variable rules
module-rgx=(([a-z_][a-z0-9_]*)|([a-z][a-zA-Z0-9]+))$
module-name-hint=(([a-z_][a-z0-9_]*)|([a-a][a-z0-9]+))$

# Only magic functions does not require docstring
no-docstring-rgx=__.*__

# Docstring must have at least one line of documentation
docstring-min-length=1

[DESIGN]

# Maximum number of branch for function / method body
max-branches=7

# Maximum number of statements in function / method body
max-statements=50

# Maximum number of parents for a class (see R0901).
max-parents=7

[EXCEPTIONS]

# Exceptions that will emit a warning when being caught. Defaults to
# "Exception"
overgeneral-exceptions=Exception,BaseException

[MASTER]

# blacklisted files or directories.
ignore=CVS,setup.py,tests

[TYPECHECK]

# List of module names for which member attributes should not be checked
# (useful for modules/projects where namespaces are manipulated during runtime
# and thus existing member attributes cannot be deduced by static analysis
ignored-modules=sh

[CLASSES]
exclude-protected=_invalidate,_emit

[MESSAGE CONTROL]
disable=fixme