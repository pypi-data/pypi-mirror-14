# -*- coding: utf-8 -*-
"""Configuration options for the Propertyshelf {{{ theme.name }}}."""

PROFILE_ID = u'profile-{{{ package.dottedname }}}'
INSTALL_PROFILE = '{0}:default'.format(PROFILE_ID)
UNINSTALL_PROFILE = '{0}:uninstall'.format(PROFILE_ID)
PROJECT_NAME = '{{{ package.dottedname }}}'
