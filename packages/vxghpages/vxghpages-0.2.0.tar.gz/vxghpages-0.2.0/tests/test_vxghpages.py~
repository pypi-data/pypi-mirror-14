
from click.testing import CliRunner

from vxghpages.cli import main


def test_main():
    runner = CliRunner()
    result = runner.invoke(main, ['--version'])

    assert result.output.find('Version') > 0
    assert result.exit_code == 0
