from setuptools import setup, find_packages

setup(
    name='dummy-pool',
    version='0.0.26',
    author='tonic',
    zip_safe=False,
    author_email='tonic@wolege.ca',
    description='pool',
    py_modules=['pool'],
    packages=find_packages(),
)
